============
Installation
============

At the command line::

    $ easy_install mongo-conduction

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv mongo-conduction
    $ pip install mongo-conduction

`Install MongoDB`_ too.

.. _Install MongoDB: http://docs.mongodb.org/manual/installation/
